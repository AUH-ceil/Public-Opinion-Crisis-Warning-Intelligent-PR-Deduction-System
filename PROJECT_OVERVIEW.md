# 舆情危机预警与智能公关推演系统 — 项目面试指南

## 一句话概述

> 基于 LangGraph 多智能体协作 + RAG 向量检索 + LLM 的舆情分析系统。输入品牌名，自动完成"搜索舆情 → 情感分析 → 历史案例匹配 → 竞品推演 → 风险量化 → 公关策略生成"全链路，每步由独立的 AI Agent 执行。

---

## 一、项目解决了什么问题

品牌在社交媒体上被吐槽时，公关团队面临三个难题：
1. **信息散落**：微博、小红书、抖音、知乎……负面评价分散在各个平台，人工收集效率低
2. **经验断层**：新的危机跟历史上某品牌的情况很像，但团队里没人记得当年怎么处理的
3. **决策缺乏依据**：到底严不严重？要不要公开回应？竞品会怎么反应？全靠拍脑袋

本系统用 AI Agent 自动化解决这三个问题。

---

## 二、系统架构（面试时可以画这个图）

```
用户输入: "麻辣小王子"
              │
              ▼
    ┌─────────────────────────┐
    │  品牌理解（LLM）          │  ← 分析这是什么品类、消费者聊什么
    │  "辣条零食品牌，讨论口味   │
    │   辣度、性价比"           │
    └──────────┬──────────────┘
               │ 生成5个搜索词
               ▼
    ┌─────────────────────────┐
    │  Agent 1: 爬虫Agent      │  ← SerpAPI/Playwright 真实搜索
    │  搜索微博/小红书/新闻...   │     + LLM 提取结构化舆情
    └──────────┬──────────────┘
               │ 20条舆情
               ▼
    ┌─────────────────────────┐
    │  Agent 2: 情感分析Agent   │  ← LLM 逐条打分，识别反讽/段子
    │  分值 + 负面关键词 + 风险标签│
    └──────────┬──────────────┘
               │
       ┌───────┴───────┐
       ▼               ▼         ← 并行执行
  ┌─────────┐    ┌──────────┐
  │RAG 检索  │    │MySQL 查询 │
  │ Qdrant  │    │ 结构化数据 │
  │语义匹配  │    │ 分类补充   │
  └────┬────┘    └─────┬────┘
       │               │
       └───────┬───────┘
               ▼
    ┌─────────────────────────┐
    │  Agent 3: 竞品推演Agent   │  ← LLM 推演竞品可能的反击
    │  "卫龙可能借势营销..."     │
    └──────────┬──────────────┘
               ▼
    ┌─────────────────────────┐
    │  风险计算器              │  ← 数学公式：情感分×权重×传播热度
    │  风险指数 + 走势预测      │
    └──────────┬──────────────┘
               ▼
    ┌─────────────────────────┐
    │  Agent 4: 公关策略Agent   │  ← LLM 综合所有信息生成方案
    │  紧急+短期+长期应对方案    │     每次随机不同策略视角
    └──────────┬──────────────┘
               │
               ▼
    ┌─────────────────────────┐
    │  存入向量库（RAG 自学习） │  ← 本次分析存回 Qdrant
    │  下次搜到类似问题直接参考  │     知识库越用越丰富
    └─────────────────────────┘
```

---

## 三、技术栈

| 层级 | 技术 | 作用 |
|------|------|------|
| **编排框架** | LangGraph (StateGraph) | 7节点 DAG 工作流，自动并行+汇合 |
| **大模型** | DeepSeek (兼容 OpenAI API) | 品牌理解、情感分析、竞品推演、公关策略 |
| **向量库** | Qdrant (TF-IDF 256维) | RAG 语义检索，不可用时降级为本地内存 |
| **关系库** | MySQL (pymysql) | 媒体权重表 + 历史案例库（结构化查询） |
| **搜索引擎** | SerpAPI / Playwright | 真实搜索，API→浏览器自动降级 |
| **Web 框架** | FastAPI | REST API，7个端点 |
| **前端** | HTML + TailwindCSS + Chart.js | 单页应用，深浅色双模式 |

---

## 四、四个 Agent 各自的职责和技术细节

### Agent 1：爬虫 Agent (`crawler_agent.py`)

**职责**：获取真实舆情数据

**工作流程**：
1. 用户输入品牌名 → LLM 先分析品牌（品类/产品/消费者话题）
2. LLM 自动生成 5 个搜索词（如"麻辣小王子 好吃吗""麻辣小王子 测评"）
3. SerpAPI 真实搜索 → 不可用时浏览器自动化兜底
4. LLM 将搜索结果提取为结构化舆情（平台/作者/内容/互动数据）
5. 搜索完全不可用时 → 基于品牌品类动态生成消费者语气评价

**面试要点**：
- 不是硬编码模板，LLM 先理解品牌再决定搜什么
- 多级降级：API → 浏览器 → 动态生成，保证系统可用

### Agent 2：情感分析 Agent (`sentiment_agent.py`)

**职责**：对每条舆情做 NLP 情感分析

**工作流程**：
1. LLM 逐条分析，返回：情感分值(-1~+1)、标签(负面/中性/正向)、负面关键词、风险标签
2. 能识别反讽（"真是太棒了呢🙃"）、段子、水军
3. LLM 不可用时降级为 43 词负面词典 + 20 词正向词典的规则引擎

**面试要点**：
- 普通差评不等于危机："难吃"是负面但不触发"食品安全"风险标签
- 保留了规则引擎作为降级方案

### Agent 3：竞品推演 Agent (`competitor_agent.py`)

**职责**：推演竞品在当前危机下可能的行动

**工作流程**：
1. LLM 基于品牌品类匹配真实竞品名（不是"竞品A"占位符）
2. 推演 6 种可能的竞品动作（借势营销/价格战/沉默观望/挖角/收购等）
3. 每种动作附发生概率 + 详细推演描述 + 真实历史案例
4. 降级时从 60+ 真实竞品名 + 品类匹配库中选取

**面试要点**：业内大部分舆情系统不做竞品博弈维度，这是差异化亮点

### Agent 4：公关策略 Agent (`pr_strategy_agent.py`)

**职责**：生成多层级公关应对方案

**工作流程**：
1. LLM 综合：RAG 历史案例 + 品牌品类 + 消费者具体抱怨 + 竞品动作
2. 生成 3 套方案：紧急(24h) / 短期(1-2周) / 长期(1-6月)
3. 每次随机一个"策略视角"（社交媒体/产品改进/KOL合作/...）避免方案重复
4. 降级链：LLM → 品类动态策略 → 知识库匹配 → 通用框架

**面试要点**：
- 辣条难吃的策略 ≠ 手机爆炸的策略 ≠ 航班超售的策略
- 每次分析结果不同（9种随机视角），但都有理有据

---

## 五、数据层：三路数据如何协作

### MySQL（结构化）
- `media_weights`：12 条媒体权重记录（微博头部 2.5、抖音头部 2.8……）
- `history_records`：20 条历史舆情案例（含事件名、分类、处理方案、风险指数）
- 作用：为风险计算提供媒体权重参数，为 RAG 提供分类匹配的案例补充

### Qdrant 向量库（语义检索）
- 启动时从 MySQL 同步 20 条种子案例 → TF-IDF 256 维向量化
- 用户分析时：用当前舆情文本做语义检索，找最相似的历史案例
- 分析完成后：**结果自动存回向量库**（RAG 自学习闭环）
- Qdrant 不可用时：降级为本地内存 + JSON 文件持久化

### SerpAPI 搜索引擎（外部数据）
- 每次分析实时搜索，获取当前网络上的真实讨论
- 不做持久化（搜索结果是临时的，分析结果才持久化）

---

## 六、LangGraph 工作流（7 节点 DAG）

```
[START]
   │
   ▼
crawler ──→ sentiment ──┬──→ rag_search ──┬──→ competitor ──→ risk_calc ──→ pr_gen ──→ [END]
                         │                  │
                         └──→ mysql_query ──┘
                              ↑ 并行执行 ↑
```

| 节点 | 输入 | 输出 |
|------|------|------|
| crawler | 品牌名 | 20条舆情 |
| sentiment | 舆情列表 | 情感分值+标签+关键词 |
| rag_search | 舆情文本 | Qdrant 语义匹配 + MySQL 分类匹配 → 合并案例 |
| mysql_query | — | 媒体权重均值 + 触达人数 |
| competitor | 情感结果+风险等级 | 6个竞品动作推演 |
| risk_calc | 情感分+媒体权重+触达 | 风险指数 0-100 + 走势预测 + 热搜概率 |
| pr_gen | 全部上下文 | 紧急+短期+长期公关方案 |

并行点：`rag_search` 和 `mysql_query` 同时执行，两者都完成后才汇入 `competitor`。

---

## 七、面试话术模板

### "请介绍一下你的项目"

> 我做了一个**舆情危机预警与智能公关推演系统**。核心思路是用四个 AI Agent 组成一个分析流水线：输入一个品牌名，系统自动去网上搜索真实舆情，用 LLM 分析情感倾向，从向量库里检索历史上类似的危机案例作为参考，推演竞品可能怎么反应，最后生成分层的公关应对方案。
>
> 技术栈是 **FastAPI + LangGraph + DeepSeek + Qdrant + MySQL**。LangGraph 负责编排 Agent 工作流，Qdrant 做 RAG 语义检索，DeepSeek 提供 LLM 推理能力。

### "最大的技术挑战是什么？"

> **挑战 1：真实数据获取。** 国内社交媒体平台没有开放 API，我设计了多级降级策略——SerpAPI 搜索 → Playwright 浏览器自动化 → 基于品牌品类动态生成评价，保证各种情况下系统都能输出合理结果。
>
> **挑战 2：公关策略不能千篇一律。** 辣条被说难吃跟手机电池爆炸是完全不同的处理思路。我让 LLM 先理解品牌品类，再结合 RAG 历史案例、消费者具体抱怨、竞品动作等多个信息源来定制方案，并且每次分析随机一个"策略视角"（产品改进/KOL合作/社交媒体传播），保证多次运行不会给出雷同建议。
>
> **挑战 3：知识库自增长。** RAG 系统最大的价值是越用越聪明。我设计了一个闭环——每次分析的结果自动存入向量库，下次搜到类似问题时能直接参考之前的分析和解决方案。

### "项目还有什么可以改进的？"

> 1. 情感分析可以微调一个垂直领域的小模型替代通用 LLM，降低成本
> 2. 可以接入实时舆情流（微博/抖音的 RSS 或 Webhook），做成真正的监控系统
> 3. 前端可以加一个对话式界面，让用户直接跟公关顾问 Agent 对话
> 4. 风险预测模型可以用历史数据训练一个 XGBoost/LightGBM 模型替代当前的公式计算

---

## 八、项目文件速查

```
agent11/
├── start.py                     # 一键启动
├── .env                         # API Key + 数据库配置
├── PROJECT_OVERVIEW.md          # 本文件
├── backend/
│   ├── main.py                  # FastAPI 入口，启动时初始化所有服务
│   ├── models.py                # Pydantic 数据模型（40+类型定义）
│   ├── llm_client.py            # LLM 调用封装（DeepSeek，指数退避重试）
│   ├── search.py                # 搜索引擎（SerpAPI→Playwright 三级降级）
│   ├── database.py              # MySQL 操作（媒体权重+历史案例）
│   ├── qdrant_client.py         # Qdrant/RAG（TF-IDF向量化+本地降级+文件持久化）
│   ├── risk_calculator.py       # 风险公式+走势预测+热搜概率
│   ├── agents/
│   │   ├── crawler_agent.py     # Agent1: 品牌理解→搜索→舆情提取
│   │   ├── sentiment_agent.py   # Agent2: LLM情感分析
│   │   ├── competitor_agent.py  # Agent3: 竞品推演
│   │   ├── pr_strategy_agent.py # Agent4: 公关策略生成
│   │   └── pr_templates.py      # 公关策略知识库（按行业分类）
│   ├── api/
│   │   └── routes.py            # 7个 API 端点
│   └── workflow/
│       ├── graph.py             # LangGraph StateGraph 构建
│       └── nodes.py             # 7个节点函数
├── frontend/                    # 纯 HTML+JS 单页应用
├── data/
│   ├── init_db.sql              # SQL 建表脚本
│   ├── sample_data.py           # 独立数据填充
│   └── vector_store.json        # 向量库本地持久化文件
└── qdrant_server/               # Qdrant 可执行文件
```
